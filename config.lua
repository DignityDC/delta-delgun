Config = {}

Config.Command = 'delgun'

Config.CheckInterval = 10 -- I'd recommend keeping at 10, if u decrease it might use a lot of ur CPU % yep.

Config.Permission = 'delta.delgun' -- Permission required to use the delete gun

Config.AimToDelete = false -- If true you are not required to shoot the gun, just aim at entity

Config.EntityOutlineColor = { r = 0, g = 255, b = 255, a = 255 } -- Color the outline and line will be.